# UniHub 测试清单

## ✅ 功能测试

### 1. 全局搜索

- [ ] 按 `⌘K` / `Ctrl+K` 打开搜索框
- [ ] 按 `⌘P` / `Ctrl+P` 打开搜索框
- [ ] 点击顶部"搜索"按钮打开搜索框
- [ ] 输入关键词，实时显示搜索结果
- [ ] 使用 `↑` `↓` 键导航结果
- [ ] 按 `Enter` 打开选中的插件
- [ ] 按 `ESC` 关闭搜索框
- [ ] 搜索结果按匹配度排序
- [ ] 显示插件图标、名称、描述、分类
- [ ] 显示关键词标签

### 2. 权限系统

- [ ] 安装插件时显示权限请求对话框
- [ ] 显示所有请求的权限及描述
- [ ] 显示风险等级（高/中/低）
- [ ] 点击"取消"中止安装
- [ ] 点击"允许并安装"继续安装
- [ ] 插件调用 API 时验证权限
- [ ] 无权限时抛出错误
- [ ] 卸载插件时移除权限

### 3. 全局快捷键

- [ ] 按 `⌘⇧Space` / `Ctrl+Shift+Space` 显示主窗口
- [ ] 再次按快捷键隐藏主窗口
- [ ] 快捷键在任何应用中都有效
- [ ] 应用退出时清理快捷键

### 4. 应用内快捷键

- [ ] `⌘K` / `Ctrl+K` - 打开全局搜索
- [ ] `⌘P` / `Ctrl+P` - 打开全局搜索
- [ ] `⌘T` / `Ctrl+T` - 新建主页标签
- [ ] `⌘W` / `Ctrl+W` - 关闭当前标签
- [ ] `⌘B` / `Ctrl+B` - 切换侧边栏

### 5. 插件隔离

- [ ] 插件只能访问自己的目录
- [ ] 路径遍历攻击被阻止
- [ ] 插件数据相互隔离
- [ ] 插件崩溃不影响主程序

---

## 🔧 API 测试

### 1. 剪贴板 API（需要 clipboard 权限）

```typescript
// 测试代码
try {
  // 写入文本
  await window.unihub.clipboard.writeText('Hello UniHub')
  
  // 读取文本
  const text = await window.unihub.clipboard.readText()
  console.log('剪贴板内容:', text)
  
  // 测试通过 ✅
} catch (error) {
  console.error('测试失败:', error)
}
```

- [ ] 有权限时正常工作
- [ ] 无权限时抛出错误

### 2. HTTP API（需要 http 权限）

```typescript
// 测试代码
try {
  const data = await window.unihub.http.get('https://api.github.com')
  console.log('HTTP 响应:', data)
  
  // 测试通过 ✅
} catch (error) {
  console.error('测试失败:', error)
}
```

- [ ] 有权限时正常工作
- [ ] 无权限时抛出错误

### 3. 数据库 API（无需权限）

```typescript
// 测试代码
try {
  // 保存数据
  await window.unihub.db.set('test-key', { value: 123 })
  
  // 读取数据
  const data = await window.unihub.db.get('test-key')
  console.log('数据库内容:', data)
  
  // 删除数据
  await window.unihub.db.delete('test-key')
  
  // 测试通过 ✅
} catch (error) {
  console.error('测试失败:', error)
}
```

- [ ] 保存数据成功
- [ ] 读取数据成功
- [ ] 删除数据成功
- [ ] 数据在插件间隔离

### 4. Sidecar API（需要 spawn 权限）

```typescript
// 测试代码
try {
  const result = await window.node.spawn('./sidecar/main', [], {
    input: JSON.stringify({ action: 'test' })
  })
  console.log('Sidecar 输出:', result.stdout)
  
  // 测试通过 ✅
} catch (error) {
  console.error('测试失败:', error)
}
```

- [ ] 有权限时正常工作
- [ ] 无权限时抛出错误
- [ ] 只能执行插件目录内的文件
- [ ] 路径遍历攻击被阻止

---

## 🎨 UI/UX 测试

### 1. 全局搜索 UI

- [ ] 搜索框居中显示
- [ ] 背景半透明遮罩
- [ ] 搜索框有阴影效果
- [ ] 结果列表可滚动
- [ ] 选中项高亮显示
- [ ] 动画流畅自然
- [ ] 深色模式正常显示

### 2. 权限对话框 UI

- [ ] 对话框居中显示
- [ ] 权限列表清晰易读
- [ ] 风险等级颜色正确
- [ ] 图标显示正确
- [ ] 按钮响应正常
- [ ] 深色模式正常显示

### 3. 主界面 UI

- [ ] 侧边栏可折叠
- [ ] 标签页可切换
- [ ] 标签页可关闭
- [ ] 主题切换正常
- [ ] 响应式布局正常
- [ ] 动画流畅

---

## 🐛 边界情况测试

### 1. 搜索功能

- [ ] 空搜索词显示所有插件
- [ ] 无匹配结果显示提示
- [ ] 特殊字符搜索正常
- [ ] 中文搜索正常
- [ ] 大小写不敏感

### 2. 权限系统

- [ ] 无权限配置时默认无权限
- [ ] 空权限数组正常处理
- [ ] 无效权限被忽略
- [ ] 内置插件无权限限制

### 3. 快捷键

- [ ] 快捷键冲突时提示
- [ ] 快捷键注册失败时处理
- [ ] 多次注册同一快捷键
- [ ] 注销不存在的快捷键

### 4. 插件管理

- [ ] 安装损坏的插件包
- [ ] 安装缺少配置的插件
- [ ] 卸载不存在的插件
- [ ] 重复安装同一插件

---

## 📱 平台测试

### macOS

- [ ] 全局快捷键正常
- [ ] 应用内快捷键正常
- [ ] 窗口显示/隐藏正常
- [ ] 权限系统正常
- [ ] 搜索功能正常

### Windows

- [ ] 全局快捷键正常
- [ ] 应用内快捷键正常
- [ ] 窗口显示/隐藏正常
- [ ] 权限系统正常
- [ ] 搜索功能正常

### Linux

- [ ] 全局快捷键正常
- [ ] 应用内快捷键正常
- [ ] 窗口显示/隐藏正常
- [ ] 权限系统正常
- [ ] 搜索功能正常

---

## 🚀 性能测试

### 1. 搜索性能

- [ ] 搜索响应时间 < 100ms
- [ ] 大量插件时搜索流畅
- [ ] 内存占用正常

### 2. 权限验证性能

- [ ] 权限检查时间 < 1ms
- [ ] 不影响 API 调用性能

### 3. 快捷键响应

- [ ] 快捷键响应时间 < 50ms
- [ ] 窗口显示/隐藏流畅

---

## 📚 文档测试

### 1. 文档完整性

- [ ] README.md 更新
- [ ] QUICK_START.md 可用
- [ ] IMPROVEMENTS.md 详细
- [ ] API 文档准确

### 2. 示例代码

- [ ] 示例代码可运行
- [ ] 示例代码有注释
- [ ] 示例代码覆盖主要功能

---

## ✅ 测试结果

### 通过的测试

- [ ] 全局搜索功能
- [ ] 权限系统
- [ ] 全局快捷键
- [ ] 应用内快捷键
- [ ] 插件隔离
- [ ] API 功能
- [ ] UI/UX
- [ ] 边界情况
- [ ] 平台兼容性
- [ ] 性能
- [ ] 文档

### 发现的问题

1. 
2. 
3. 

### 待修复的 Bug

1. 
2. 
3. 

---

## 🎯 测试建议

1. **优先测试核心功能**：搜索、权限、快捷键
2. **测试边界情况**：空值、特殊字符、错误输入
3. **测试不同平台**：macOS、Windows、Linux
4. **测试性能**：大量插件、频繁操作
5. **测试文档**：按文档操作是否成功

---

## 📝 测试报告模板

```markdown
## 测试报告

**测试日期**：2024-XX-XX
**测试人员**：XXX
**测试环境**：macOS 14.0 / Windows 11 / Ubuntu 22.04

### 测试结果

- 通过：XX 项
- 失败：XX 项
- 跳过：XX 项

### 发现的问题

1. **问题描述**：XXX
   - 重现步骤：XXX
   - 预期结果：XXX
   - 实际结果：XXX
   - 严重程度：高/中/低

### 建议

1. XXX
2. XXX
```

---

**祝测试顺利！** 🎉
